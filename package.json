{
  "homepage": "http://an-imp.github.io/Mortgage-Calculator",
  "name": "calculator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prod": "next export",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "bootstrap": "^4.6.0",
    "chart.js": "^2.9.4",
    "formula-pmt": "^1.0.1",
    "next": "10.0.9",
    "react": "17.0.2",
    "react-bootstrap": "^1.5.2",
    "react-chartjs-2": "^2.11.1",
    "react-dom": "17.0.2"
  },
  "devDependencies": {
    "babel-plugin-react-html-attrs": "^3.0.5",
    "gh-pages": "^3.1.0"
  }
}
